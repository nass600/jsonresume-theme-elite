{{#if resume.basics}}
    <section class="section-contact">
        {{> components/title icon='phone' title='Contact'}}
        <ul>
            {{#if resume.basics.email}}
                <li class="icon-item clearfix">
                    <svg class="text-primary"><use xlink:href="#envelope"></use></svg>
                    <span class="text-secondary">{{resume.basics.email}}</span>
                </li>
            {{/if}}
            {{#if resume.basics.phone}}
                <li class="icon-item clearfix">
                    <svg class="text-primary"><use xlink:href="#phone"></use></svg>
                    <span class="text-secondary">{{resume.basics.phone}}</span>
                </li>
            {{/if}}
            {{#if resume.basics.location}}
                <li class="icon-item clearfix">
                    <svg class="text-primary"><use xlink:href="#map-marker"></use></svg>
                    <span class="text-secondary">
                    {{resume.basics.location.address}}
                        {{resume.basics.location.postalCode}}
                        {{resume.basics.location.city}}
                        {{resume.basics.location.region}}
                        {{resume.basics.location.countryCode}}
                </span>
                </li>
            {{/if}}
            {{#if resume.basics.website}}
                <li class="icon-item clearfix">
                    <svg class="text-primary"><use xlink:href="#link"></use></svg>
                    <a class="text-secondary" href="{{resume.basics.website}}">
                        {{removeSchema resume.basics.website}}
                    </a>
                </li>
            {{/if}}
        </ul>
        {{#if resume.basics.profiles.length}}
            <ul>
                {{#each resume.basics.profiles}}
                    <li class="icon-item clearfix">
                        <svg class="text-primary">
                            <use xlink:href="#{{sanitizeLowercase network}}"></use>
                        </svg>
                        <a class="text-secondary" href="{{url}}">
                            {{removeSchema url}}
                        </a>
                    </li>
                {{/each}}
            </ul>
        {{/if}}
    </section>
{{/if}}
