{{#if resume.skills.length}}
    <section class="section-skills">
        {{>title icon='el-tasks' title='Skills'}}
        {{#each resume.skills}}
            <div class="card">
                {{#if name}}
                    <div class="card-block">
                        <h4 class="card-title">
                            {{name}}
                            {{#if level}}
                                <span class="card-subtitle">{{level}}</span>
                            {{/if}}
                        </h4>
                    </div>
                {{/if}}
                {{#if keywords.length}}
                    <div class="card-footer">
                        <div class="card-tags" href="{{website}}">
                            <i class="el-tags text-primary"></i>
                            <div class="tag-list">
                                {{#each keywords}}
                                    <span class="tag text-secondary">{{this}}</span>
                                {{/each}}
                            </div>
                        </div>
                    </div>
                {{/if}}
            </div>
        {{/each}}
    </section>
{{/if}}
